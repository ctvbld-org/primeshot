# Task ID: 2
# Title: Refactor Styles Carousel component
# Status: pending
# Dependencies: 1
# Priority: high
# Description: Extract and adapt the existing styles carousel to work as a standalone component with event emission.
# Details:
Extract carousel from `/styles` page into a reusable component. Remove the 'Customize' button. Add swipe events and implement a context provider that emits style change events. Make sure the carousel maintains the current style selection state. Ensure carousel is responsive and anchors the floating option buttons to the current slide. Reuse existing styling but adapt for the new layout context.

# Test Strategy:
Unit test style selection events. Visual regression testing to ensure carousel appears and behaves identically to the existing one. Test swipe and scroll behaviors on touch and non-touch devices.
