---
description: User flow and state transitions for the headshot generation process
globs: src/app/app/**/*.tsx, src/components/**/*.tsx
alwaysApply: true
---
- **User Flow Stages**
  - Each stage corresponds to specific routes and components
  - Flow enforces sequential progression through required steps
  - State management tracks completion of each stage

  ```typescript
  // Stage status types
  type FlowStage = 
    | 'shoot'         // Creating styles
    | 'payment'       // Payment processing
    | 'upload'        // Uploading user photos
    | 'review'        // Image quality review
    | 'albums'        // Results viewing
  ```

- **1. Authentication (Route: /auth)**
  - Required before accessing any app features
  - Handled by Supabase authentication
  - Redirects to shoot page on success
  ```typescript
  // ✅ DO: Check auth state before rendering protected routes
  if (!user) return <RedirectToAuth />
  
  // ❌ DON'T: Allow access to protected routes without auth
  if (!user) renderProtectedContent() // Wrong
  ```

- **2. Profile Check (Route: /app/settings/profile)**
  - Required before accessing any app features
  - If user has gender = null, redirects to profile settings
  - Must complete profile before accessing other pages
  - Profile includes:
    - eye_color
    - hair_color
    - hair_length
    - hair_style
    - age
    - body_type
    - height
    - weight
    - ethnicity
    - glasses
  ```typescript
  // ✅ DO: Check profile completion
  if (!user.gender) return <RedirectToProfile />
  ```

- **3. Styles Page (Route: /app/shoot)**
  - Initial state shows empty grid with "New Style" button
  - Lists all draft styles for the user
  - "Next" button enabled only with at least one style
  - Can navigate to style creation page
  ```typescript
  // ✅ DO: Filter styles by status
  .eq('status', 'draft')
  
  // ✅ DO: Enable next button conditionally
  disabled={styles.length === 0}
  ```

- **4. Style Creation (Route: /app/style)**
  - Uses style_configs table for available options
  - Style configuration includes:
    - Photography Style (studio, natural, dramatic)
    - Outfit (professional, casual, creative)
    - Background (plain, office, outdoor, custom)
    - Gender-specific options
    - Clothing colors
  - Can save or go back to shoot page
  - Saves as draft status in styles table
  ```typescript
  // ✅ DO: Save with draft status
  status: 'draft' as StyleStatus
  
  // ✅ DO: Format name consistently
  `${photographyStyle} ${outfit} ${background}`
  ```

- **5. Payment (Route: /app/payment)**
  - Only accessible with at least one style
  - Shows order summary with selected styles
  - Creates order record in orders table
  - Handles payment processing via Stripe
  - Updates style status on successful payment
  ```typescript
  // ✅ DO: Update all styles on payment
  styles.map(style => updateStatus(style.id, 'processing'))
  
  // ✅ DO: Show clear payment feedback
  paymentStatus, paymentError
  ```

- **6. Payment Success (Route: /app/payment/success)**
  - Only accessible after successful payment
  - Shows success message
  - Provides button to proceed to upload
  - Cannot go back to previous stages
  - Updates order status in orders table

- **7. Photo Upload (Route: /app/upload)**
  - Only accessible after successful payment
  - Cannot go back to previous stages
  - Uses upload_sessions and upload_chunks tables for chunked upload
  - Validates image requirements:
    - Minimum resolution
    - Face detection
    - Image quality checks
  ```typescript
  // ✅ DO: Create upload session
  await supabase.from('upload_sessions').insert({
    user_id: user.id,
    order_id: orderId,
    file_name: file.name,
    file_size: file.size,
    total_chunks: calculateChunks(file.size)
  })
  
  // ✅ DO: Show upload progress
  uploadProgress, uploadStatus
  ```

- **8. Upload Review (Route: /app/review)**
  - Shows upload results with quality ratings from images table
  - Can go back to upload page only
  - Allows re-upload of rejected images
  - Requires all images to pass quality check
  - Initiates AI generation process
  ```typescript
  // ✅ DO: Show clear rejection reasons
  image.rejectionReasons.map(reason => ...)
  
  // ✅ DO: Enable continue only when all images valid
  disabled={hasRejectedImages}
  ```

- **9. Albums (Route: /app/albums)**
  - Shows all generated images grouped by style
  - Uses generated_images table to track:
    - Processing status
    - Storage paths
    - Error messages
    - Metadata
  - Cannot go back to any previous stages
  ```typescript
  // ✅ DO: Group by style and status
  const groupedImages = groupBy(generatedImages, ['style_id', 'status'])
  
  // ✅ DO: Sort by creation date
  orderBy(group, 'created_at', 'desc')
  ```

- **State Transitions**
  ```mermaid
  graph LR
    A[Draft] --> B[Processing]
    B --> C[Completed]
    B --> D[Failed]
    D --> A
  ```

- **Error Handling**
  - Each stage should handle:
    - Network errors
    - Validation failures
    - Payment failures
    - Processing errors
    - Upload failures
  ```typescript
  // ✅ DO: Show user-friendly error messages
  toast({
    title: 'Error',
    description: getErrorMessage(error),
    variant: 'destructive'
  })
  ```

- **Navigation Rules**
  - Forward progression requires completion of previous steps
  - Back navigation allowed to any previous step
  - Style editing only allowed in draft status
  ```typescript
  // ✅ DO: Check step completion
  if (!hasCompletedStep) redirect('/previous-step')
  
  // ✅ DO: Disable editing for non-draft styles
  disabled={style.status !== 'draft'}
  ```

- **Session Persistence and State Recovery**
  - Track and restore user's progress through the flow
  - Persist state for users who haven't completed payment
  - Automatically redirect to last active step on return
  - Uses user_progress table with flow_stage type

  ```typescript
  // Flow progress tracking type
  type FlowProgress = {
    currentStage: FlowStage
    lastActiveAt: string
    completedStages: FlowStage[]
    stageData: {
      compositions?: {
        savedCount: number
        lastSavedAt?: string
      }
      upload?: {
        uploadedFiles: string[]
        uploadProgress: number
        lastUploadAt?: string
      }
      review?: {
        reviewedImages: string[]
        rejectedImages: string[]
        lastReviewAt?: string
      }
      payment?: {
        attemptCount: number
        lastAttemptAt?: string
      }
    }
  }
  ```

  ```typescript
  // ✅ DO: Save progress after each significant action
  await supabase
    .from('user_progress')
    .upsert({
      user_id: user.id,
      current_stage: 'upload',
      completed_stages: ['shoot'],
      stage_data: stageData,
      last_active_at: new Date().toISOString()
    })

  // ✅ DO: Check and restore progress on page load
  useEffect(() => {
    const { data: progress } = await supabase
      .from('user_progress')
      .select()
      .eq('user_id', user.id)
      .single()

    if (progress && !isPaymentCompleted) {
      router.push(`/app/${progress.current_stage}`)
    }
  }, [user])

  // ❌ DON'T: Allow skipping to future stages
  if (!progress.completedStages.includes('shoot')) {
    return redirect('/app/shoot')
  }
  ```

- **Progress Persistence Points**
  1. **Shoot Stage**
     - After landing on empty shoot page
     - After each style save
     - Before proceeding to payment
     ```typescript
     // ✅ DO: Update progress after style save
     await updateProgress({
       currentStage: 'shoot',
       stageData: {
         compositions: {
           savedCount: styles.length,
           lastSavedAt: new Date().toISOString()
         }
       }
     })
     ```

  2. **Upload Stage**
     - On upload page load
     - During file upload (progress)
     - After upload completion
     ```typescript
     // ✅ DO: Track upload progress
     await updateProgress({
       currentStage: 'upload',
       stageData: {
         upload: {
           uploadedFiles: [...existingFiles, newFile],
           uploadProgress: (uploadedCount / totalFiles) * 100,
           lastUploadAt: new Date().toISOString()
         }
       }
     })
     ```

  3. **Review Stage**
     - Initial image review status
     - After each image rejection/approval
     - Before proceeding to generation
     ```typescript
     // ✅ DO: Save review decisions
     await updateProgress({
       currentStage: 'review',
       stageData: {
         review: {
           reviewedImages: approvedImages,
           rejectedImages: rejectedImages,
           lastReviewAt: new Date().toISOString()
         }
       }
     })
     ```

  4. **Payment Stage**
     - Payment page load
     - Payment attempt tracking
     ```typescript
     // ✅ DO: Track payment attempts
     await updateProgress({
       currentStage: 'payment',
       stageData: {
         payment: {
           attemptCount: progress.stageData.payment.attemptCount + 1,
           lastAttemptAt: new Date().toISOString()
         }
       }
     })
     ```

- **Progress Cleanup**
  - Clear progress data after successful payment
  - Retain historical data in completed_user_journeys table
  ```typescript
  // ✅ DO: Clear progress after payment
  if (paymentSuccess) {
    await supabase
      .from('user_progress')
      .delete()
      .eq('user_id', user.id)
    
    // Archive for analytics
    await supabase
      .from('completed_user_journeys')
      .insert({
        user_id: user.id,
        journey_data: progress,
        completed_at: new Date().toISOString()
      })
  }
  ```

- **State Recovery Rules**
  - Always validate stage access against completed stages
  - Prevent direct URL access to future stages
  - Maintain data consistency across page reloads
  - Handle browser back/forward navigation
  ```typescript
  // ✅ DO: Validate stage access
  export function validateStageAccess(
    currentStage: FlowStage,
    progress: FlowProgress
  ) {
    const stageOrder = [
      'shoot',
      'payment',
      'upload',
      'review',
      'albums'
    ]
    
    const currentIndex = stageOrder.indexOf(currentStage)
    const completedIndex = Math.max(
      ...progress.completedStages.map(stage => 
        stageOrder.indexOf(stage)
      )
    )
    
    return currentIndex <= completedIndex + 1
  }
  ```

- **Related Rules**
  - See [auth.mdc](mdc:.cursor/rules/auth.mdc) for authentication patterns
  - See [project_structure.mdc](mdc:.cursor/rules/project_structure.mdc) for file organization
  - See [supabase_migrations.mdc](mdc:.cursor/rules/supabase_migrations.mdc) for database schema