// Stub for website - returns data as-is since translation happens elsewhere
export function useTranslatedScenes(scenes: any[]) {
  return scenes
}

export function useTranslatedWardrobes(wardrobes: any[]) {
  return wardrobes
}

export function useTranslatedColors(colors: any[]) {
  return colors
}

